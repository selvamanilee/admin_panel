import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Admin.Panel.Project.WebUI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
